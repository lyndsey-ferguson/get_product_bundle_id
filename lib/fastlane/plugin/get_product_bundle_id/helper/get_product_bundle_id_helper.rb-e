module Fastlane
  module Helper
    class GetProductBundleIdHelper
      # class methods that you define here become available in your action
      # as `Helper::GetProductBundleIdHelper.your_method`
      #
      def self.show_message
        UI.message("Hello from the get_product_bundle_id plugin helper!")
      end
    end
  end
end
